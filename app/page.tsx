'use client';

import { useCallback, useEffect, useMemo, useState } from 'react';
import type { Prospect } from '@/lib/types';
import { prospectSignalLabels, prospectSignals, type ProspectSignalTier } from '@/lib/prospect-signals';
import ProspectMap from '@/components/ProspectMap';

const label = (value: string | null | undefined) => value?.replaceAll('_', ' ') || 'Unknown';
const signalFor = (prospect: Prospect) => prospectSignals(prospect);

export default function Dashboard() {
  const [all, setAll] = useState<Prospect[]>([]); const [selected, setSelected] = useState<Prospect | null>(null);
  const [query, setQuery] = useState(''); const [city, setCity] = useState(''); const [type, setType] = useState(''); const [tier, setTier] = useState('');
  const [demo, setDemo] = useState(false); const [searching, setSearching] = useState(false); const [notice, setNotice] = useState('');
  const loadProspects = useCallback(async () => { const response = await fetch('/api/prospects'); const payload = await response.json(); setAll(Array.isArray(payload.prospects) ? payload.prospects : []); setDemo(Boolean(payload.demo)); if (payload.error) setNotice(payload.error); }, []);
  useEffect(() => { void loadProspects(); }, [loadProspects]);
  const list = useMemo(() => all.filter((prospect) => (!city || prospect.city === city) && (!type || prospect.facility_type === type) && (!tier || signalFor(prospect).tier === tier) && `${prospect.name} ${prospect.address}`.toLowerCase().includes(query.toLowerCase())).sort((a, b) => signalFor(b).score - signalFor(a).score), [all, city, type, tier, query]);
  const metrics: [string, string | number][] = [['Listed facilities', list.length], ['Strong operating signals', list.filter((item) => signalFor(item).tier === 'strong_operating_signal').length], ['Listed 24/7', list.filter((item) => signalFor(item).is24Hour).length], ['Ready to call', list.filter((item) => Boolean(item.phone && item.website)).length]];
  async function findProspects() { setSearching(true); try { const response = await fetch('/api/prospects/search', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ centerLatitude: 40.8, centerLongitude: -77.8, radiusKm: 300, state: 'Pennsylvania', categories: ['industrial', 'manufacturing', 'warehouse'], limit: 250 }) }); const result = await response.json(); if (!response.ok) { setNotice(result.error || 'Live search failed.'); return; } setNotice(`Search complete: ${result.imported ?? 0} imported. New listings are automatically scored from their business-profile signals.`); await loadProspects(); } catch { setNotice('Unable to reach the prospect search service. Please try again.'); } finally { setSearching(false); } }
  const selectedSignals = selected ? signalFor(selected) : null;

  return <main className="min-h-screen"><nav className="bg-[#101d29] text-white px-6 py-4 flex justify-between items-center"><div><b className="tracking-wide text-lg">GRIDSWITCH</b><span className="ml-3 text-sm text-slate-300">Prospecting Dashboard</span></div>{demo && <span className="rounded-full bg-slate-700 px-3 py-1 text-xs">Demo Data</span>}</nav><div className="p-5 max-w-[1800px] mx-auto"><div className="mb-4"><h1 className="text-xl font-bold">Find facilities worth a GridSwitch discovery call</h1><p className="text-sm text-slate-600 mt-1">Ranking is automatic from the live business profile: facility category, listed 24/7 hours, logistics cues, related listings, and available contact details. It is not an electricity-use estimate.</p></div><div className="grid grid-cols-2 lg:grid-cols-4 gap-3 mb-4">{metrics.map(([title, value]) => <div className="card p-4" key={title}><div className="text-xs uppercase text-slate-500">{title}</div><div className="text-2xl font-bold mt-1">{value}</div></div>)}</div><div className="card p-3 flex gap-2 flex-wrap mb-4"><input className="control flex-1 min-w-48" placeholder="Search facility or address" value={query} onChange={(event) => setQuery(event.target.value)} /><select className="control" value={city} onChange={(event) => setCity(event.target.value)}><option value="">All cities</option>{[...new Set(all.map((item) => item.city).filter(Boolean))].map((item) => <option key={item}>{item}</option>)}</select><select className="control" value={type} onChange={(event) => setType(event.target.value)}><option value="">All facility types</option>{[...new Set(all.map((item) => item.facility_type).filter(Boolean))].map((item) => <option key={item} value={item!}>{label(item)}</option>)}</select><select className="control" value={tier} onChange={(event) => setTier(event.target.value)}><option value="">All prospect signals</option>{Object.entries(prospectSignalLabels).map(([value, text]) => <option key={value} value={value}>{text}</option>)}</select><button className="bg-teal-700 text-white rounded px-4 text-sm" disabled={searching} onClick={findProspects}>{searching ? 'Searching…' : 'Find Prospects'}</button></div>{notice && <p className="text-sm mb-3 text-teal-800">{notice}</p>}<div className="grid grid-cols-1 xl:grid-cols-[minmax(420px,1fr)_minmax(600px,1.2fr)] gap-4 h-[680px]"><section className="card overflow-hidden min-h-96"><ProspectMap prospects={list} selected={selected} onSelect={setSelected} /></section><section className="card overflow-auto"><table className="w-full"><thead className="sticky top-0 bg-white"><tr>{['Prospect signal', 'Facility', 'Type', 'City', 'Why it is listed', 'Hours', 'Contact', 'Status'].map((heading) => <th key={heading}>{heading}</th>)}</tr></thead><tbody>{list.map((prospect) => { const signals = signalFor(prospect); return <tr className={`cursor-pointer hover:bg-slate-50 ${selected?.id === prospect.id ? 'bg-teal-50' : ''}`} key={prospect.id} onClick={() => setSelected(prospect)}><td><span className={`score ${signals.score >= 65 ? 'score-high' : signals.score >= 40 ? 'score-mid' : 'score-low'}`}>{prospectSignalLabels[signals.tier]}</span></td><td className="font-medium">{prospect.name}</td><td className="capitalize">{label(prospect.facility_type)}</td><td>{prospect.city}</td><td>{signals.reasons.slice(0, 2).join(' · ') || 'Business listing'}</td><td>{signals.is24Hour ? '24/7 listed' : 'Not listed'}</td><td>{prospect.phone && prospect.website ? 'Phone + site' : prospect.phone ? 'Phone' : prospect.website ? 'Website' : 'Unavailable'}</td><td className="capitalize">{prospect.prospect_status}</td></tr>; })}</tbody></table></section></div>{selected && selectedSignals && <aside className="fixed right-0 top-0 h-full w-[440px] bg-white shadow-2xl p-6 overflow-auto z-10"><button className="float-right text-slate-500" onClick={() => setSelected(null)}>Close</button><h2 className="text-xl font-bold pr-12">{selected.name}</h2><p className="mt-2"><span className="score score-high">{prospectSignalLabels[selectedSignals.tier]}</span> <span className="text-slate-600">Prospecting signal: {selectedSignals.score}/100</span></p><p className="text-sm mt-4">{selected.address}, {selected.city}, {selected.state} {selected.postal_code}</p><section className="mt-6"><h3 className="font-semibold">Why this is worth a discovery call</h3><ul className="mt-3 space-y-2 text-sm">{selectedSignals.reasons.length ? selectedSignals.reasons.map((reason) => <li key={reason} className="flex gap-2"><span className="text-teal-700">●</span>{reason}</li>) : <li>No additional profile signals are available yet.</li>}</ul></section><section className="mt-6"><h3 className="font-semibold">Business-profile details</h3><dl className="grid grid-cols-2 gap-y-4 mt-3 text-sm"><div><dt className="text-slate-500">Business categories</dt><dd className="font-medium">{selectedSignals.categories.join(', ') || 'Not available'}</dd></div><div><dt className="text-slate-500">Listed operating hours</dt><dd className="font-medium">{selectedSignals.is24Hour ? '24/7' : 'Not available'}</dd></div><div><dt className="text-slate-500">Operations cues</dt><dd className="font-medium">{selectedSignals.logisticsCues.map((cue) => cue.replace(/\b\w/g, (letter) => letter.toUpperCase())).join(', ') || 'None listed'}</dd></div><div><dt className="text-slate-500">Related listings</dt><dd className="font-medium">{selectedSignals.relatedListings || 'None listed'}</dd></div><div><dt className="text-slate-500">Phone</dt><dd className="font-medium">{selected.phone || 'Not available'}</dd></div><div><dt className="text-slate-500">Public registry check</dt><dd className="font-medium">{selected.epa_frs_id || selected.pa_dep_facility_id ? 'Matched' : selected.public_records_verified_at ? 'No confident match' : 'Optional; not needed to rank'}</dd></div></dl></section><section className="mt-6 rounded bg-slate-50 p-3 text-xs text-slate-600"><b>Use this for outreach prioritization:</b> call strong operating signals first, then ask about operating hours, outage exposure, critical loads, expansion, backup generation, and utility constraints. The signal is not a claim about electricity demand or microgrid fit.</section><div className="flex gap-2 mt-6 flex-wrap"><button className="control" onClick={() => navigator.clipboard.writeText(`${selected.address}, ${selected.city}, ${selected.state}`)}>Copy address</button>{selected.phone && <a className="control" href={`tel:${selected.phone}`}>Call facility</a>}{selected.website && <a className="control" target="_blank" rel="noreferrer" href={selected.website}>Open website</a>}</div></aside>}</div></main>;
}
